#
# Cookbook Name:: {{app.name}} v{{app.version}}
# Recipe:: lib
#
# {{app.publisher.packaging_copyright}}
#
# Exists only to load the libraries associated with the cookbook.
# Otherwise a no-op.
#

puts "{{app.name}}::lib -- {{app.name}} v{{app.version}} library loaded"